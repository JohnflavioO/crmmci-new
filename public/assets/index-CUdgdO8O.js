// Compatibility shim for the currently published hashed entry file.
// If an old Lovable/editor HTML shell is cached, this redirects it to the
// stable entry emitted as /assets/index.js after the next build.
import("./index.js");